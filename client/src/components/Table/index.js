import Header from './Header';
import Row from './Row';
import './table.css';

const Contacts = () => {
  return (
    <div className="users-table-container">
      <Header />
      <Row />
    </div>
  );
};

export default Contacts;
